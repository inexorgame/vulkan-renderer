#pragma once

#include <array>

namespace inexor::vulkan_renderer {

/// The following data will be replaced by CMake setup.
constexpr const char *APP_NAME{"${APP_NAME}"};
constexpr std::array<std::uint32_t, 3> APP_VERSION{${APP_VERSION_MAJOR}, ${APP_VERSION_MINOR},
                                                           ${APP_VERSION_PATCH}};
constexpr const char *APP_VERSION_STR{"${APP_VERSION_MAJOR}.${APP_VERSION_MINOR}.${APP_VERSION_PATCH}"};
constexpr const char* ENGINE_NAME{"${ENGINE_NAME}"};
constexpr std::array<std::uint32_t, 3> ENGINE_VERSION{${ENGINE_VERSION_MAJOR}, ${ENGINE_VERSION_MINOR},
                                                      ${ENGINE_VERSION_PATCH}};
constexpr const char *ENGINE_VERSION_STR{"${ENGINE_VERSION_MAJOR}.${ENGINE_VERSION_MINOR}.${ENGINE_VERSION_PATCH}"};
constexpr const char *BUILD_GIT = "${GIT_SHA}";
constexpr const char *BUILD_TYPE = "${BUILD_TYPE}";

} // namespace inexor::vulkan_renderer
